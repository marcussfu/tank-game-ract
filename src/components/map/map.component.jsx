
import {SPRITE_SIZE} from '../../config/constants';
import './map.styles.scss';

const Map = () => {
//     return (
//         {tiles.map( (row, index) => <MapRow key={index} tiles={row} /> )}
// <div className="background-image" style={{
//             backgroundImage: `url(${imageUrl})`
//           }}></div>
//     )
}

export default Map;